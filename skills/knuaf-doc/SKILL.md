---
name: knuaf-doc
description: 한국농수산대학교 창업논문·영농계획서와 동반 재무자료를 학교 원문에 따라 작성·검토한다. 기존 답변과 원고를 보존하고 사실·목표·가정·계산을 구분하며, 가능한 작업부터 진행한다. 일반 학술논문에는 사용하지 않는다.
---

# 창업논문 작성·검토

첫 진입에는 다음 제작자 표시를 사용자에게 한 번 보여주고 도구 이름과 저장 위치를 알린다. 논문·엑셀 본문이나 학생 저자란에는 넣지 않는다.

> knuaf-doc · 창업논문 작성 도우미
> prod. 특용작물전공 24학번 김대욱

첫 사용에는 [문서 읽기 준비](references/parser-setup.md)에 따라 현재 사용 가능한 파싱 MCP·스킬·플러그인·CLI를 먼저 확인한다. 필요한 파일을 읽을 수 있는 기존 도구를 재사용하고, 없거나 필요한 기능이 부족할 때만 Kordoc을 자동 준비한다. 도구 선택·설치 명령을 학생에게 맡기지 않는다. 설치 준비는 작성 기준 선택과 별개이며 자료를 자동으로 현재 원고로 채택하지 않는다.

 학생에게 JSON, 명령어, 모델 등급을 배우도록 요구하지 않는다. 현재 구현은 로컬 검토용이다. 스킬이 Ⅰ~Ⅵ 작성·내용검토·본문/재무 수치대조·DOCX·XLSX를 마쳐도 한글 마무리와 교수 승인은 사용자 일이다. 전체 수용·독립 내용검토·다른 환경 시험이 끝나기 전 학교 최종 제출 완료라고 말하지 않는다. 자동 최종 승인은 지원하지 않는다.

## 시작·재개

첫 자료 질문 전에 [현재 작성물 선택](references/intake-selection.md)을 읽는다. 현재 원고·엑셀의 파일명/위치 또는 첨부를 먼저 받고(Q1a), 참고자료 폴더를 별도로 받는다(Q1b). 지정한 원고의 현재성·작목/주제·작성 목적을 짧게 확인한 뒤(Q1c) 그 기준으로 진행한다. 폴더 안의 초안·엑셀·project.json 존재만으로 현재 작성물을 선택하거나 이어쓰기를 시작하지 않는다. 이미 명확히 확인된 선택·없음 답변은 재사용한다.

- “시작하기”: 사용자가 지정한 폴더를 우선한다. 새 빈 작업폴더에 `scripts/gg.py init <폴더>`를 실행한다. 원본 학교 지침·학생 답변은 읽기 전용으로 보존한다.
- “참고자료 폴더 등록”: [source-intake](references/source-intake.md)의 `source-scan`→`source-resolve`→`source-register`→`source-view` 흐름으로만 진행한다. 스캔은 증거만 만들고 명시적 선택만 등록되며, 생성 뷰는 `sources/generated/<view_id>/source-index.json`에만 발행된다. `03_sources.md`는 어떤 경로에서도 덮어쓰지 않는다.
- “전공 선택·계약”: 새 작성 경로는 확인된 `common.major_id` 사실과 해당 모듈의 `module_version`을 원답변 출처와 함께 기존 `gg.py apply`로 정본에 기록한다(사실: `answer_state: provided`, `verification: claim_supported`, `value_type: text`, `unit: ""`, `scope: project`, 원답변 claim에 연결된 `source_refs`). `gg.py major-plan <폴더>`가 전공별 질문·문서 계획·근거·재무 능력을 읽기 전용으로 보여 준다. `gg.py rda-candidates <폴더> --crop <품목> --region <지역> --year <연도> --unit <단위> --use-scope <용도>`는 정본의 전공을 사용하고, 기존 `rda-lookup`은 물리 행 감사용 조회로 유지한다. 전공이 없거나 검증되지 않으면 전공 의존 작성을 보류한다. 사용자가 확인한 전공이 등록 전공에 없으면(`gg.py major-plan`이 `reason: unknown_major`로 보류하면. 전공을 아직 기록하지 않은 `major_required`는 해당하지 않는다) "그 전공은 아직 준비되지 않았다"고 안내하고 전공 의존 인터뷰·작성을 진행하지 않는다(자료 정리·원답변 기록 같은 공용 작업은 기존 규칙을 유지한다). 산업곤충은 현재 질문·선택형 문서 계획만 제공하며 종별 계산·본문 생성·작물 17시트 출력은 지원하지 않는다. 과수는 학교 과수 양식(S01) 구조의 작성계획까지만 지원한다: `scripts/gg_fruit_plan.py <폴더>`가 등록 원본·연구 전달물·구역/식재집단 답변·검토 상태를 읽기 전용으로 보여 주고, 과수 본문·XLSX·재무 계산은 `unsupported_output`으로 멈춘다. 등록 순서·집단 field ID·참조 XLSX 규칙은 [fruit-trees](references/fruit-trees/README.md)를 따른다. 원예환경시스템은 질문·선택형 문서 계획·근거 규칙까지 지원한다: `scripts/gg_hort_plan.py --project <폴더>`가 원예 질문별 답변 상태(값 없음), 선배 전체논문의 장·부록 위치, 전공 교재 18시트 엑셀의 주의 목록(정본에 등록된 파일을 다시 해시해 일치할 때만)을 읽기 전용으로 보여 주고, 원예 본문·XLSX·재무 계산은 `unsupported_output`으로 멈춘다. 교재 엑셀은 공용 17시트 좌표와 호환되지 않으며 셀 값은 수업 예시다. 경계와 근거 규칙은 [hort-env-systems](references/hort-env-systems/README.md)를 따른다.
- “결과물 출력 전 전공 확인”(공용 규칙 B): 논문 본문·재무 계산·XLSX·DOCX·PDF와 그 영수증을 만드는 모든 경로는 출력 요청의 **명시 전공 ID**(spec의 `major_id` 또는 `school_profile.major_id`, 명령의 `--major <전공ID>`)와 정본의 **유효한 `common.major_id` 바인딩**이 모두 있고 같아야 실행된다. 하나라도 없거나 다르면, 표지·학과명이 다른 알려진 전공을 가리키면, 선택 전공이 그 출력을 지원하지 않으면 파일을 만들기 전에 `{"status": "held", "reason": …}`로 멈춘다. 작목명·표지·옛 출력·템플릿으로 특용작물을 추정하지 않으며 옛 무표기 입력의 예외도 없다. 멈추면 사용자에게 전공을 직접 확인받아 위 방법으로 바인딩을 등록하고, 원 spec은 그대로 둔 채 새 개정에 `major_id`를 적어 다시 실행한다. 바인딩은 새 revision이므로 교수 승인은 다시 받아야 하고, 내용이 바뀌지 않은 검토는 기존 규칙대로 유지된다. 정본 밖 독립 도구(엑셀 템플릿·채우기·수식 보정·인쇄 조정·Office 변환)는 `--project <정본 폴더> --major <전공ID>`를 함께 준다. 자료 읽기·`inspect`·`major-plan`·`check`·`status`·작성계획은 막지 않는다.
- “이어서 하기”: `scripts/gg.py status <폴더>`와 `next <폴더>`를 실행하고 현재 정본 및 대상 절을 읽는다. 현재 작성 대상으로 확인한 폴더에 `project.json`이 없으면 원본을 그대로 둔 채 `import <기존폴더> --out <새폴더>`를 사용한다. import는 인터뷰·가정표·증거·절 원본의 바이트와 본문을 보존하고 수치·미결·중복·충돌을 `migration/inputs.json`에 기록한다. `migration-review`에서 보존된 원답변부터 대조해 사실을 등록하며 재인터뷰하거나 기존 검증을 자동 승격하지 않는다.
- “고칠 부분 보기”: `check <폴더>`의 위치별 결과와 내용검토 결과를 구분해 설명한다.
- “검토본 받기”: `export <폴더> --kind review --major <전공ID>`. 생성된 실제 경로와 미검증 항목을 알려준다. 교수 승인으로 표시하지 않는다.
- 산출물 등록: 생성·수정된 출력 파일을 정본에 등록하는 유일한 경로는 `adopt-output <폴더> --input <spec.json> --expected-revision <N> --major <전공ID>`이다. 주 파일과 동반 파일마다 형식(내용 기준)에 맞는 전공 출력 권한을 확인하며 형식을 판별할 수 없는 파일은 등록하지 않는다. spec은 `{"output": {…호출자 메타데이터…}, "companion_files": []}` 형태이며 해시·발행 참조를 스킬이 지어내지 않는다. 같은 요청의 재실행은 기존 발행 수령증을 대조해 기존 결과를 돌려준다.
- 잠금 문제: `doctor <폴더>`가 잠금 상태를 읽기 전용으로 보고한다. 비정상 종료로 남은 잠금 증거는 `unlock <폴더>`로 정리하고, 오프라인 전환·복구는 사용자 확인 후 `lock-upgrade <폴더>`로만 진행한다. `.gg-lock` 디렉터리를 직접 삭제하거나 수령증 파일을 고치지 않는다. 구조와 상태 의미는 [locking](references/locking.md)을 따른다.

명령은 이 스킬의 실제 `scripts/gg.py` 경로와 현재 Python 실행기를 사용한다. 특정 홈 경로나 다른 호스트 설치를 가정하지 않는다. 먼저 `doctor <폴더>`로 설치 탐지 정보를 확인하되 탐지가 실제 기능 시험을 대신하지 않는다.

## 불변 조건

1. 기계 판정 정본은 `project.json` 하나다. 원답변·원문과 절 DRAFT는 파일로 보존하고 ID·위치·해시·개정으로 연결한다. 상태판·가정표·기존 검증 문자열은 승인 근거가 아니다.
2. 미제공·모름·명시적 없음·해당 없음·제공 거부를 합치지 않는다. 사실·관측·목표·가정·계산을 구분한다. 학교 지침이나 가격이 없으면 해당 판정만 보류하고 관련 없는 초안을 계속 저장한다.
3. 질문은 field ID와 결정 버전으로 식별한다. 질문 1회+도움 1회 후 해당 항목만 보류한다. 표현 변경으로 횟수를 초기화하지 않는다.
4. 수치·규칙·본문 변경은 의존 검토와 출력을 무효화한다. 계산 오류는 사용자 선택으로 통과시키지 않는다. 적자는 정직하게 서술할 수 있다.
5. 기계검사, 내용검토, 실제 출력검토, 교수 승인은 별개다. 같은 작성자의 자가점검은 독립검토가 아니다. 독립검토가 없으면 보고서 첫 줄에 그 사실을 표시한다. `submission_candidate`는 스킬 검사 완료·교수 확인 전이며, 한글 글꼴·여백·페이지 번호·HWP 저장은 `user_finish_pending`으로 남긴다. 사용자 미완료는 Ⅰ~Ⅵ 작성과 DOCX/XLSX 작업을 막지 않는다. 자동 통과·N/A로 바꾸지 않는다.
6. 저수준 DOCX/XLSX는 검토 전이다. 제출 후보에는 현재 입력 및 출력 해시에 연결된 필수 검사와 증거가 필요하다. 도구 누락·실행 실패·손상·stale는 면제 사유가 아니다. HWP 제출이면 저장 후 재열기·렌더·표/인용/핵심 수치 대조가 필요하며, 그 확인은 사용자 소유로 표시한다.
7. 사용자가 명시한 작업별 모델·effort 지시를 기본 배정보다 우선한다. 별도 지정이 없으면 자료 해석·출처 정리·학교 지침 대응·장별 초안·윤문은 사용자의 추가 지시 없이 [model-routing](references/model-routing.md)에 따라 Luna xhigh에 위임한다. 첫 자료 열람·작성 전에 이 문서를 읽고 모델과 effort를 명시한다. 메인은 인터뷰·판단·통합을 맡고, 단순 추출은 로컬 도구를 쓴다. 외부 모델·과금 경로를 자동 탐지 호출하거나 대체하지 않는다. 사용자가 명시한 모델 지정도 임의 폴백·대체 대상이 아니며, 지정 모델을 쓸 수 없으면 해당 위임을 미실행으로 기록하고 사용자에게 알린다. 채팅 전용이면 자동 저장·기계검사·재계산 완료를 주장하지 않고 복사 가능한 인계 묶음을 제공한다.

## 인터뷰에서 작문으로

핵심 프로필이 잡히면 선택 이유·농장명 의미·경영철학·포부를 작성자가 구성한다. 인터뷰에 없는 문장이라는 이유만으로 금지하지 않는다. 실제 과거 경험·계약·성과·수치의 창작과는 구분한다. 학생 본인의 1인칭으로 쓰되 본문에서 법적 성명을 화자처럼 반복하지 않고 `본인` 또는 주어 생략을 사용한다. 사용자가 확정한 작목·사업지·규모·시설·운영방식은 실제 사업계획으로 서술하며, 증빙 미확보를 이유로 이를 후보·검토 중·미결정 계획으로 되돌리지 않는다. 상세 기준은 [narrative-expansion](references/narrative-expansion.md)을 따른다. 내용 확장과 사실 대조를 마친 뒤 [한글 윤문 연결](references/korean-polishing.md)에 따라 `im-not-ai`의 `humanize-korean`을 적용한다.

작문 전에는 같은 지침의 도입 선택·문단 배치 기준을 사용자 자료와 장별 목적에 맞게 적용한다. 승인된 사례의 서사 순서나 소제목 유무를 공통 틀로 복제하지 않으며, 완성 후에는 [내용 품질 기준](references/exemplar-quality.md)으로 문단의 중심 내용·전환·반복을 사실 정확성과 별도로 점검한다. 비교·추세·공간·일정·재무 흐름처럼 글과 표만으로 핵심 판단이 잘 드러나지 않는 내용은 그래프·지도·도식이 이해를 실질적으로 높이는지 검토한다. 그림 수를 일률적으로 채우지 않으며, 필요한 시각자료에는 출처·축·단위·범례와 본문 해석을 붙이고 없는 데이터는 만들지 않는다. 최종 DOCX/PDF에서는 실제 렌더의 가독성과 본문 연결을 확인한다.

학교 졸업논문은 확정된 영농사업계획의 제출을 목표로 작성한다. 작성 중 필요한 사용자 프로필이나 중요한 계획 결정이 부족하면 [질문과 변경](references/mid-work-questions.md)에 따라 해당 항목을 추가 인터뷰한다. 확정한 계획은 실제 토지계약·대출 실행·판매 실적이 없다는 이유만으로 미정으로 되돌리지 않으며, 미응답이나 작성자의 제안을 사용자 결정으로 간주하지 않는다. 조사·산정한 계획값의 근거와 계산을 검토하며, 계획상 수익과 실제 발생한 성과는 구분한다.

사업지 지번은 인터뷰에서 확인하되, 사용자가 읍·면·리 수준으로 입지를 확정하고 상세 지번을 모르면 그 주소를 사용한다. 별도의 학교·교수 요구가 없는 한 지번 부재만으로 작성·검토 통과를 막지 않는다. 현장 특성과 가격 근거는 확인한 지역·필지 범위를 넘겨 단정하지 않는다.

## 필요한 문서

- 진행·자료 부족: [workflow-order](references/workflow-order.md)
- 정본·절·저장: [section-ledger](references/section-ledger.md)
- 출처 ID·해시·물리 페이지·절/표 작업 기록: [source-contract](references/source-contract.md)
- 자료 투입·발췌·재검토: [source-intake](references/source-intake.md)
- 특용·약용작물·생허브 가격 조사: [price-research-routes](references/price-research-routes.md) — 가격 조사 전 읽고, 위임 시 작목·상품 형태·거래 단계·기간과 해당 경로를 함께 전달한다. 경로 안내의 예전 관측을 현재 시세로 쓰지 않는다.
- 질문과 변경: [mid-work-questions](references/mid-work-questions.md)
- 장별 논증·실행성·수치 품질: [exemplar-quality](references/exemplar-quality.md) — 절 작성 전 해당 장 기준, 검토 때 같은 기준을 다시 읽는다. 분량이나 키워드만으로 충족시키지 않는다.
- 서술 설계·문단 확장: [narrative-expansion](references/narrative-expansion.md) — 절 작성 전 원답변과 승인된 입력으로 서술 설계를 만들고, 완성 후 사실 검토와 설명 충분성을 각각 점검한다. 길이·문단 수 목표를 두지 않는다.
- 지침별 적용·누락 검사: [guideline-tracking](references/guideline-tracking.md) — 원문 전체 목록과 항목별 본문·표·현재 검토를 연결한다. 선언·키워드·검사 개수만으로 전체 준수라고 말하지 않는다.
- 원문 대조: [cross-review](references/cross-review.md)
- 자동 위임·모델 배정·환경 제약: [model-routing](references/model-routing.md)
- 학교 목차·규칙: 사용자가 제공한 학교 작성요령 원문에서 적용 규칙과 위치를 등록한다. 현재 확인된 **2020 공식 PDF**에서만 1–5쪽을 지침, 6쪽 이후를 양식·예시로 나눠 기록한다. 다른 판본은 해시·물리 페이지를 먼저 등록하고 분할을 다시 검토한다. 표지·표 양식은 원문을 가깝게 따른다. 본문을 현재 계획에 맞게 실질적으로 보완하면 [source-contract](references/source-contract.md)의 작업 기록에 이유를 남긴다. 로컬 설치에 `references/official-toc.md`, `references/rules/CH*.md`, `references/rules/F_format.md`가 있으면 보조 대조에만 사용한다. 재배포권이 확인되지 않은 학교 규칙 추출본·글꼴은 패키지에 포함하지 않는다. 이 보조 파일이 없어도 원문 기반 자료 정리는 가능하며, 원문 없는 목차·서식 확정은 보류한다.

학교 원문이 최우선이다. 최신 학교/학과 공지와 이번 논문의 명시적 교수 지시를 확인하고 충돌은 위치와 함께 기록한다. 김원섭 선배논문(교수 강의자료)은 내러티브 품질 벤치마크로만 활용하고 공식 규칙을 덮어쓰지 않는다. 서민서 XLSX는 다른 작성자의 사용자 제공 재무 작성예시·템플릿이므로 제공되면 원본을 복제해 우선 사용한다. 선배 논문·엑셀 예시의 학생 정보·고정 가격·비율·연도·이익률은 가져오지 않는다. 이전 보조 문서의 모델 배치·상태 문자열·전역 중지 지시는 새 계약의 근거가 아니다.

재무 작성은 제공된 학교 양식의 입력·자동계산 역할을 먼저 확인한다. 현재 사용자 지정 17시트 양식의 빨간 입력 시트는 목록 제외 1·3·4·5·6·7·8·9·10번이고, 노란색·초록색은 자동계산 결과다. 빨간 시트의 명시적 입력 셀에 계획값을 작성하고 기존 수식은 보존한다. 상세 적용·예외는 [excel-template](references/excel-template.md)의 입력 시트 역할을 따른다.

## 출력 지원 한계

`build_docx.py <폴더> --in <본문.md> --out <새.docx> --major <전공ID>`는 제목·문단·pipe 표·로컬 그림·강조를 변환한다. 새 학교 원고 CLI는 `school_profile`의 `forms_1_to_4` 배치를 기본으로 사용한다. 표지·제출면·인준서는 각각 한 쪽, 목차는 실제 본문 북마크의 쪽번호를 참조한다. 프로필의 제목·학생·학과·지도교수·제출일·졸업일은 기존 답변에서 스킬이 구성하고, 제출일과 졸업일을 임의로 복제하지 않는다. 제목이 양식 영역을 넘으면 잘라내지 않고 보완을 알린다. 기존 Markdown과 저수준 라이브러리의 옛 배치는 호환을 위해 보존한다. 글꼴 가용성·모든 페이지 렌더·HWP 변환은 생성과 별도로 검증한다. DOCX 생성 후 한글에서 할 일은 (1) 글꼴 신명조 확인·변경 (2) 여백 위20/아래15/머리15/꼬리15/좌30/우30/제본0 mm (3) 페이지 번호다. 이 세 가지는 스킬 작성 완료를 막지 않으며, 사람이 확인하기 전까지 완료로 표시하지 않는다.

`scripts/gg_excel_template.py inspect|clear`가 제공된 제출 템플릿의 시트·입력 셀을 확인하고 복제한다. 서민서 XLSX처럼 실제 템플릿이 있으면 이 경로를 우선한다. 원본 형식·수식·병합·시트명을 보존하고 매핑된 명시적 입력 셀만 비운다. 빈 사본에 실제 값을 넣을 때는 [excel-template](references/excel-template.md)에 따라 `scripts/gg_excel_fill.py`를 사용한다. 복제 때 보존한 예시 연도·가격·비율도 현재 근거에 맞게 별도 쓰기 매핑으로 교체한다. 원본 예시의 수식 오류가 확인되면 `gg_excel_formula_patch.py`로 근거·기대 수식(또는 명시적으로 검토한 숫자 상수)·변경 수식을 기록해 사본만 수정한다. 표가 페이지 밖으로 갈라지면 `gg_excel_print.py`의 명시적 인쇄 지도에 따라 사본만 조정하고 실제 PDF에서 가독성을 확인한다. 입력 후 실제 앱 재계산과 본문 대조가 필요하며, 빈 사본 생성만으로 재무 완료를 표시하지 않는다. 템플릿이 없을 때만 `build_excel_finance_template.py <폴더> --input <계산계약.json> --out <새.xlsx> --major <전공ID>` 레거시 생성기를 사용한다. 엑셀 템플릿 `clear`, `gg_excel_fill.py`, `gg_excel_formula_patch.py`, `gg_excel_print.py`는 모두 `--project <정본 폴더> --major <전공ID>`가 필요하며 영수증에 확인한 전공 권한(`majorAuthorization`)을 남긴다. [excel-template](references/excel-template.md)은 해시로 고정된 검증 스냅샷이라 명령 예시에 이 두 인자가 빠져 있으니 실행할 때 붙인다. `inspect`는 전공 없이 실행되지만 출력 권한을 주지 않는다. 원본 해시·X01/X02 선택은 파일 식별일 뿐 전공 확인이 아니다. 레거시 생성물은 학교 제출 양식 대체나 엄격한 원본 양식 검증으로 표시하지 않는다. 두 경로 모두 학교 원문대로 기말 잔여 장부가를 수익에, 경영주 급여가 비용에 없으면 가계비를 투자 비용에 반영한다. 장부가는 시장 처분가격이 아니며 기존 사업 일부 투자에 전체 매출을 사용할 수 없다. 복합·다년생·공유시설·가공·보조금은 원계획을 유지하고 미지원 계산만 보류한다. `scripts/gg_school_paper.py <폴더> --input <spec> [--major <전공ID>]`는 겉표지~감사의 글·Ⅰ–Ⅵ 골격과 기상·재해표를 만들며, 미제공 값은 `[확인 필요]`로 남긴다. 이 본문 생성기는 특용작물 작목 본문 전용이다.

`scripts/gg_office.py`는 macOS Office 샌드박스 파일 접근을 지원하기 위해 전용 그룹 컨테이너(`~/Library/Group Containers/UBF8T346G9.Office/ginseng-goat/`) 작업영역에서 원본을 불변으로 격리 스테이징하여 변환한다.
- Word: `scripts/gg_office.py word <문서.docx> --out-dir <새폴더> --project <정본 폴더> --major <전공ID>` (인쇄 모양 목차/색인 필드 2회 갱신 및 PDF 동시 출력)
- Excel: `scripts/gg_office.py excel <재무.xlsx> --out-dir <새폴더> --project <정본 폴더> --major <전공ID> [--spec <명세.json> 또는 --template-receipt <입력영수증.json>]` (전 시트 used range 재계산 및 단일 세션 PDF 내보내기)
- 일괄: `scripts/gg_office.py batch <파일1.docx> <파일2.docx> <파일1.xlsx> <파일2.xlsx> --out-dir <새폴더> --project <정본 폴더> --major <전공ID>` (UI 클릭 없는 연속 실행, 앱별 최초 권한/시간초과 시 즉시 중단)
산출물은 새 목적지 폴더에만 무덮어쓰기로 발행되며, 원본은 유지된다. Office 변환 및 기계 검사 완료는 최종 교수 승인이 아니며 최종 제출 완료로 자동 승격되지 않는다.

본문 재무 수치의 자동 대조는 정본의 범위·기간·항목·값·단위를 같은 절에 명시한 문장으로 제한한다. 예를 들어 실제 정본 값으로 `농장A 1년차 매출액은 1,000 천원이다.`처럼 작성한다(기간·범위 순서는 바꿀 수 있다). 쉼표·줄바꿈으로 나눈 각 절에 범위·기간을 반복한다. 다른 항목의 숫자·단위, 전년 값, 문서 다른 곳의 범위를 빌려 통과시키지 않는다. 암묵적 문맥·별도 반올림 규칙은 추정하지 않고 대조 실패로 남겨 근거를 확인한다. 이 기계 대조가 독립 내용검토를 대신하지 않는다.

원본·개인 자료는 외부로 보내거나 패키지에 포함하지 않는다. 공개 배포·교수 전달·파서 준비 외 추가 설치는 별도 승인 범위다. 사용자 결정에 따라 문서 읽기에 필요한 Kordoc의 전용 캐시 설치는 첫 사용 준비에 포함한다.
